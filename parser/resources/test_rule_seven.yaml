- name: test rule
  patterns: 
    - id: cdde2e49-dc42-40be-bfae-1baa3c4583fa
      name: aws json
      type: regex 
      partition: root
      pattern: '^aws: (?P<json>{.*})'
      triggers:
        - name: json
          action: forward
          format: json
          partition: aws json
